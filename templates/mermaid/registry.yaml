# Mermaid Diagram Template Registry
# Central version registry for all parameterized diagram templates.
# When a template is refined, bump its version and add a changelog entry.
# All consuming specs should be re-rendered after a version bump.

templates:
  procurement-lifecycle:
    current: "1.0"
    changelog:
      "1.0": "Initial procurement lifecycle flow extracted from 01900 UI/UX spec"
    parameters:
      - discipline
      - complexity
      - showHITL
      - showPayment

  three-state-navigation:
    current: "1.0"
    changelog:
      "1.0": "Initial three-state navigation flow for discipline UI/UX specs"
    parameters:
      - discipline
      - states
      - buttons

  order-creation-flow:
    current: "1.0"
    changelog:
      "1.0": "Initial order creation with spec template registry selection, discipline inheritance, and task generation"
    parameters:
      - discipline
      - showRegistry

  sow-generation:
    current: "1.0"
    changelog:
      "1.0": "AI-enhanced SOW generation with spec template selection per appendix and HITL gates"
    parameters:
      - discipline
      - appendices

  hitl-review:
    current: "1.0"
    changelog:
      "1.0": "HITL review with AI assessment, reviewer assignment, decision framework, and audit trail"
    parameters:
      - discipline
      - confidenceThreshold

  approval-matrix:
    current: "1.0"
    changelog:
      "1.0": "Progressive approval matrix with value-based routing"
    parameters:
      - discipline
      - thresholds

  spec-template-registry:
    current: "1.0"
    changelog:
      "1.0": "Spec Template Registry flow with admin, selection, AI generation, and HITL review"
    parameters:
      - discipline
      - appendixLetter

  appendix-b-hitl:
    current: "1.0"
    changelog:
      "1.0": "Template-driven appendix HITL flow with dynamic schema rendering"
    parameters:
      - discipline
      - templateId

  template-complexity-tree:
    current: "1.0"
    changelog:
      "1.0": "Complexity level decision tree for procurement type"
    parameters:
      - discipline

  page-state-flow:
    current: "1.0"
    changelog:
      "1.0": "Page state flow with auth, discipline selection, and modal routing"
    parameters:
      - discipline
      - states
      - modals

  agents-state-flow:
    current: "1.0"
    changelog:
      "1.0": "Agents state navigation with modal transitions"
    parameters:
      - discipline
      - showDetails
      - showRemove

  upsert-state-flow:
    current: "1.0"
    changelog:
      "1.0": "Upsert state navigation with record creation, editing, import"
    parameters:
      - discipline
      - showBulkImport

  workspace-state-flow:
    current: "1.0"
    changelog:
      "1.0": "Workspace state with HITL review, approval, rejection transitions"
    parameters:
      - discipline
      - showAssign
      - showReport

  hitl-workflow:
    current: "1.0"
    changelog:
      "1.0": "HITL review with AI agent, queue, approval/rejection, and LoRA feedback"
    parameters:
      - discipline
      - confidenceThreshold
      - showKnowledgeLoop

  cad-viewer-flow:
    current: "1.0"
    changelog:
      "1.0": "CAD viewer flow with loading, parsing, rendering, and validation"
    parameters:
      - discipline
      - showValidation
      - showExport

  engineering-design-flow:
    current: "1.0"
    changelog:
      "1.0": "Multi-phase engineering design flow"
    parameters:
      - discipline
      - phaseCount

  structural-engineering-flow:
    current: "1.0"
    changelog:
      "1.0": "Structural engineering workflow with FEA"
    parameters:
      - discipline
      - showFEA

  mep-engineering-flow:
    current: "1.0"
    changelog:
      "1.0": "MEP systems engineering flow"
    parameters:
      - discipline
      - showPerformance

  document-generation-flow:
    current: "1.0"
    changelog:
      "1.0": "AI-powered document generation workflow"
    parameters:
      - discipline
      - showValidation

  logistics-lifecycle:
    current: "1.0"
    changelog:
      "1.0": "Logistics lifecycle from need through delivery and HITL review"
    parameters:
      - discipline
      - showCustoms

  logistics-order-creation:
    current: "1.0"
    changelog:
      "1.0": "Logistics order creation with template selection and agent assignment"
    parameters:
      - discipline