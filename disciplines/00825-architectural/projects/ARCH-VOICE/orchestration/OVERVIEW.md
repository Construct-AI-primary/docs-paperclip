# ARCH-VOICE — Orchestration Overview

## Architecture

```
ARCH-VOICE (Architect — 00825-architectural)
    └── references: ENG-VOICE (Shared Engineering Override)
            └── extends: VOICE-COMM (Shared Core)
```

This project is a discipline-level reference that consumes the shared ENG-VOICE override with `disciplineCode = "00825"`.
